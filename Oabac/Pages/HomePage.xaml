<Page
    x:Class="Oabac.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Oabac.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Spacing="20" Padding="20">
            <StackPanel.ChildrenTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition FromVerticalOffset="50" IsStaggeringEnabled="True"/>
                </TransitionCollection>
            </StackPanel.ChildrenTransitions>
            <TextBlock Text="Dashboard" Style="{StaticResource TitleTextBlockStyle}"/>

            <Grid Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="10">
                    <TextBlock Text="Sync Control" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <Button Click="SyncNow_Click" Style="{StaticResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE895;"/>
                            <TextBlock Text="Sync Now"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="10">
                    <TextBlock Text="CONFIGURED SYNC FOLDERS" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <ListView x:Name="MappingsList">
                        <ListView.ItemContainerTransitions>
                            <TransitionCollection>
                                <AddDeleteThemeTransition/>
                                <ContentThemeTransition/>
                                <ReorderThemeTransition/>
                                <EntranceThemeTransition IsStaggeringEnabled="True"/>
                            </TransitionCollection>
                        </ListView.ItemContainerTransitions>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Spacing="8" Padding="0,8">
                                    <TextBlock Text="{Binding SourcePath}" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <FontIcon Glyph="&#xE72A;" FontSize="12"/>
                                        <TextBlock Text="{Binding DestinationPath}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ProgressBar Value="{Binding Progress}" Maximum="100" Height="4" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" Text="{Binding DataTransferInfo}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </Grid>
                                    <TextBlock Text="{Binding Status}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <TextBlock x:Name="EmptyStateText" Text="No sync mappings configured..." Visibility="Collapsed"/>
                </StackPanel>
            </Grid>

            <Grid Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="10">
                    <TextBlock Text="Activity Log" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBox x:Name="LogBox" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" Height="200" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
